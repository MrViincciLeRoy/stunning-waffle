# Core data handling and scientific computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Machine Learning & Preprocessing
scikit-learn>=1.0.0
imbalanced-learn>=0.9.0
lightgbm>=3.3.0

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# File I/O and utilities
requests>=2.25.0
joblib>=1.0.0

# Progress tracking
tqdm>=4.60.0

# Transit modeling (optional but recommended)
batman-package>=2.4.7

# GPU acceleration (optional)
# cupy-cuda11x>=10.0.0  # Uncomment if using CUDA 11.x
# cudf>=21.08          # Uncomment if using RAPIDS

# Jupyter (for notebook-style cells, if needed)
# jupyter>=1.0.0

# Note: The code uses `numba-cuda` via `uv pip install`, but this is typically handled via conda or system CUDA.
# Standard numba can be installed via pip; CUDA support requires additional setup.
numba>=0.56.0

# For JSON encoding of NumPy types (used in config saving)
# (Included in numpy, no extra package needed)
